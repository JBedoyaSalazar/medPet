const appendToSheet = async (data) => {
  console.log("Datos para guardar en Google Sheets:", data);
  // Aquí debes implementar la lógica real de Google Sheets más adelante en el curso.
};

export default appendToSheet;
