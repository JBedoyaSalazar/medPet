const openAiService = async (message) => {
  console.log("Mensaje para OpenAI:", message);
  return "Esta es una respuesta simulada del asistente inteligente. (Falta implementar OpenAI).";
};

export default openAiService;
